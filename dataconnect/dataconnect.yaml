specVersion: "v1"
serviceId: "radio-station-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "radio-station-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
